name: Check generated files

on:
  workflow_dispatch:
  push:
    branches:
      - master
  pull_request:

jobs:
  gen-layouts:
    name: Layout list
    runs-on: ubuntu-latest
    steps:
    - name: Checkout repo  
      uses: actions/checkout@v4
    - run: python3 gen_layouts.py
    - name: "Run 'python3 gen_layouts.py' to update the generated files."
      run: git diff --exit-code

  check-layouts:
    name: Layout warnings
    runs-on: ubuntu-latest
    steps:
    - name: Checkout repo
      uses: actions/checkout@v4
    - run: python3 check_layout.py
    - name: "Fix your layout accordingly or run 'python3 check_layout.py' to promote the warnings"
      run: git diff --exit-code

  check-compose:
    name: Compose key data
    runs-on: ubuntu-latest
    steps:
    - name: Checkout repo
      uses: actions/checkout@v4
    - run: python3 srcs/compose/compile.py srcs/compose/{*.json,compose} > srcs/juloo.keyboard2/ComposeKeyData.java
    - name: "Run 'gradle test' to update the generated files."
      run: git diff --exit-code
import hashlib
import time

# بصمة السيادة الخاصة بك
SECRET = "Habibati Jami"

def broadcast_resonance():
    # توليد نبضة مشفرة بناءً على الوقت الحالي
    pulse = hashlib.sha256((SECRET + str(int(time.time()/60))).encode()).hexdigest()[:12]
    print(f"--- [ بوابة السيادة الرقمية ] ---")
    print(f"[*] نبضة الرنين النشطة: {pulse}")
    print(f"[*] الحالة: الاتصال مستقر عبر سحابة جوجل.")

if __name__ == "__main__":
    broadcast_resonance()
